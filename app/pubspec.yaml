name: nuvempark_app
description: "NuvemPark — Operação de Pátio (offline-first, multi-tenant)"
publish_to: none
version: 1.0.0+9

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Pacote compartilhado vendorizado (Dio factory, SecureStorage, ApiException, widgets base)
  nuvempark_core:
    path: packages/nuvempark_core

  # Estado
  flutter_riverpod: ^3.3.1
  # HTTP
  dio: ^5.9.2
  # Roteamento
  go_router: ^17.3.0
  # Banco local offline-first
  drift: ^2.22.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.4
  path: ^1.9.0
  # Storage seguro (token, device uuid)
  flutter_secure_storage: ^10.3.1
  # Utils
  crypto: ^3.0.5
  uuid: ^4.5.3
  connectivity_plus: ^7.0.0
  intl: ^0.20.2
  # QR (leitura na saída / geração no cupom de entrada)
  mobile_scanner: ^7.2.0
  qr_flutter: ^4.1.0
  # Impressão térmica ESC/POS
  print_bluetooth_thermal: ^1.1.3
  # Permissões (BT/câmera)
  permission_handler: ^11.3.1
  # Identidade do aparelho (ANDROID_ID + fabricante/modelo/SO) — não exige
  # permissão Android. Enviado só no login; o hash sha256 é feito no servidor.
  device_info_plus: ^11.3.0
  # Câmera própria (preview + moldura-guia + foco/zoom na captura de placa)
  camera: ^0.11.0
  # Foto de entrada — fallback quando a câmera própria falha (câmera do sistema)
  image_picker: ^1.2.3
  # OCR de placa on-device
  google_mlkit_text_recognition: ^0.15.1
  # Decode/crop/orientação em memória (recorte da ROI antes do OCR)
  image: ^4.3.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.22.0
  build_runner: ^2.4.13
  flutter_launcher_icons: ^0.14.4

# sqlite3 3.3.4 tem um bug de hash nos native-assets nesta toolchain
# ("Hash of downloaded file libsqlite3.arm.android.so ... expected ...").
# Pinamos em 3.3.2 (a versão que o leve-patio usa e builda OK).
dependency_overrides:
  sqlite3: 3.3.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/

  # Plus Jakarta Sans — a fonte do Brisa. EMBUTIDA de propósito, não via
  # `google_fonts`: aquele pacote baixa a fonte em runtime, e este app roda o
  # dia inteiro sem rede num tablet de pátio — cairia no fallback e o redesign
  # simplesmente não apareceria. TTFs estáticos (não o variable font): o Flutter
  # só dirige o eixo `wght` via `fontVariations`, então com variable font todo
  # `fontWeight:` do app viraria fake bold. Licença em assets/fonts/OFL.txt.
  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-Regular.ttf
          weight: 400
        - asset: assets/fonts/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/PlusJakartaSans-Bold.ttf
          weight: 700
        - asset: assets/fonts/PlusJakartaSans-ExtraBold.ttf
          weight: 800

# Branding NuvemPark — verde
flutter_launcher_icons:
  android: true
  # iOS ligado em 31/07/2026, junto da primeira compilação para iPhone — até
  # então o app carregava o ícone padrão do Flutter no aparelho.
  #
  # `remove_alpha_ios` + `background_color_ios` existem porque o iOS REJEITA
  # ícone com transparência, e o nosso tem: os cantos arredondados do
  # app_icon.png são transparentes. O verde da marca preenche esses cantos, e o
  # próprio iOS aplica a máscara arredondada por cima.
  ios: true
  remove_alpha_ios: true
  background_color_ios: "#16A34A"
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 23
  adaptive_icon_background: "#16A34A"
  adaptive_icon_foreground: "assets/icon/app_icon.png"
